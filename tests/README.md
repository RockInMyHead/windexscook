# Тесты системы AI шеф-повара

Этот каталог содержит всесторонний набор тестов для системы AI шеф-повара с голосовым интерфейсом.

## Структура тестов

```
tests/
├── backend/              # Тесты backend API
├── frontend/             # Тесты frontend компонентов
│   ├── components/       # Тесты React компонентов
│   ├── services/         # Тесты сервисов (OpenAI, email, etc.)
│   ├── utils/           # Тесты утилит (browser compatibility)
│   └── pages/           # Тесты страниц
├── integration/         # Интеграционные тесты
├── e2e/                # End-to-end тесты
├── performance/        # Тесты производительности
└── setup.js            # Настройка тестового окружения
```

## Основные функции, покрытые тестами

### 🎯 **Голосовое взаимодействие**
- Распознавание речи (Web Speech API)
- Синтез речи (OpenAI TTS)
- Обработка голосовых команд
- Управление состоянием разговора

### 🤖 **AI шеф-повар**
- Обработка запросов рецептов
- Генерация ответов через OpenAI
- Streaming ответов
- Управление контекстом разговора

### 💳 **Платежная система**
- Проверка статуса платежей
- Подтверждение платежей
- Обработка ошибок платежей
- Восстановление платежей из localStorage

### 📧 **Email сервисы**
- Отправка уведомлений
- Welcome письма
- Подтверждения платежей
- Обработка ошибок отправки

### 🌐 **Совместимость браузеров**
- Проверка поддержки MediaRecorder
- Fallback для Safari
- Проверка Web Speech API
- Обработка ограничений браузеров

## Запуск тестов

### Все тесты
```bash
npm test
```

### С покрытием кода
```bash
npm run test:coverage
```

### Только frontend тесты
```bash
npm run test:frontend
```

### Только backend тесты
```bash
npm run test:backend
```

### Интеграционные тесты
```bash
npm run test:integration
```

### End-to-end тесты
```bash
npm run test:e2e
```

### Тесты производительности
```bash
npm run test:performance
```

## Настройка окружения

### Переменные окружения для тестов

Создайте `.env.test` файл:

```env
# OpenAI
VITE_OPENAI_API_KEY=your-test-api-key

# Email (опционально для тестов)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test

# Database (для интеграционных тестов)
DATABASE_URL=sqlite::memory:
```

### Mock сервисы

Для E2E тестов может потребоваться запуск mock сервисов:

```bash
# Mock SMTP сервер (для email тестов)
npm run mock:smtp

# Mock OpenAI API (для offline тестирования)
npm run mock:openai
```

## Категории тестов

### 🔧 Unit Tests (Модульные тесты)
- Тестируют отдельные функции и компоненты
- Быстрые и изолированные
- Мокают внешние зависимости

### 🔗 Integration Tests (Интеграционные тесты)
- Тестируют взаимодействие между компонентами
- Проверяют API endpoints
- Тестируют database операции

### 🌐 E2E Tests (End-to-End тесты)
- Тестируют полный пользовательский сценарий
- Используют Puppeteer для браузерной автоматизации
- Проверяют реальное взаимодействие пользователя

### ⚡ Performance Tests (Тесты производительности)
- Проверяют скорость работы функций
- Тестируют нагрузку и concurrency
- Мониторят использование памяти

## Отчеты о покрытии

После запуска `npm run test:coverage` генерируется HTML отчет в `coverage/lcov-report/index.html`

### Минимальные требования к покрытию:
- Statements: 80%
- Branches: 75%
- Functions: 85%
- Lines: 80%

## Непрерывная интеграция

### GitHub Actions
Тесты автоматически запускаются при:
- Push в main/master
- Pull requests
- Ручной запуск

### Локальная разработка
```bash
# Запуск тестов в watch режиме
npm run test:watch

# Запуск конкретного теста
npm test -- --testNamePattern="VoiceCallNew"
```

## Моки и фикстуры

### Mock данные

**Тестовые рецепты:**
```javascript
const mockRecipe = {
  id: 'borsch-recipe',
  title: 'Классический украинский борщ',
  ingredients: [...],
  instructions: [...],
  prepTime: 30,
  cookTime: 120,
  servings: 6
};
```

**Тестовые платежи:**
```javascript
const mockPayment = {
  id: 'test-payment-123',
  status: 'succeeded',
  amount: { value: '1000.00', currency: 'RUB' },
  metadata: { userId: 'test-user' }
};
```

### Audio фикстуры
Для тестов аудио используются mock файлы в `tests/fixtures/audio/`

## Обработка ошибок

### Таймауты
- Unit тесты: 5000ms
- Integration тесты: 10000ms
- E2E тесты: 30000ms
- Performance тесты: 60000ms

### Retry логика
Тесты автоматически повторяются при flaky failures:
- Network timeouts
- Race conditions
- Browser automation issues

## Отладка тестов

### Debug режим
```bash
# Запуск с отладкой
npm test -- --verbose --detectOpenHandles

# Debug конкретного теста
npm test -- --testNamePattern="VoiceCallNew" --verbose
```

### Visual debugging (E2E)
```bash
# Запуск E2E с видимым браузером
HEADLESS=false npm run test:e2e
```

## Метрики качества

### Производительность
- Время загрузки страницы: < 3s
- Время ответа API: < 500ms
- Время обработки голоса: < 2s
- Память: < 100MB при нагрузке

### Надежность
- Uptime: > 99.5%
- Error rate: < 0.1%
- Test success rate: > 95%

## Расширение тестов

### Добавление новых тестов

1. **Unit тест:**
```javascript
describe('NewComponent', () => {
  test('should render correctly', () => {
    // test code
  });
});
```

2. **Integration тест:**
```javascript
describe('API Integration', () => {
  test('should handle requests', async () => {
    const response = await request(app)
      .post('/api/endpoint')
      .send(testData)
      .expect(200);
  });
});
```

3. **E2E тест:**
```javascript
test('user flow', async () => {
  await page.goto('/page');
  await page.click('button');
  // assertions
});
```

## Troubleshooting

### Распространенные проблемы

**Тесты падают из-за таймаутов:**
- Увеличьте таймаут в jest.config.js
- Проверьте сетевые соединения
- Убедитесь что сервисы запущены

**E2E тесты не работают:**
- Проверьте что браузер установлен
- Очистите кэш браузера
- Проверьте разрешения на камеру/микрофон

**Покрытие низкое:**
- Добавьте тесты для новых функций
- Убедитесь что тесты покрывают error cases
- Проверьте что моки не скрывают реальный код

---

## Контакты

По вопросам тестирования обращайтесь к команде разработки.